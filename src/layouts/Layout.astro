---
import "../styles/global.css";
---

<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <title>THEING Studio CyberPhonk Portfolio</title>

    <!-- Tipografías modernas -->
    <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@500;700&family=Montserrat:wght@400;600&display=swap" rel="stylesheet" />
  </head>
  <body>
    <slot />
  </body>
</html>

<style>
  :root {
    --neon-pink: #004cff;
    --neon-blue: hsl(180, 53%, 22%);
    --neon-purple: #450383;
    --accent: #d1973f;
    --dark-bg: #0a0a0f;
  }

  html,
  body {
    margin: 0;
    width: 100%;
    height: 100%;
    font-family: "Montserrat", system-ui, sans-serif;
    color: #e0e0e0;
    background: radial-gradient(circle at top left, #0a0a0f, #1a1a2e, #000000);
    background-size: 400% 400%;
    animation: cyberGradient 15s ease infinite;
    overflow-x: hidden;
  }

  @keyframes cyberGradient {
    0% { background-position: 0% 50%; }
    50% { background-position: 100% 50%; }
    100% { background-position: 0% 50%; }
  }

  ::selection {
    background: var(--neon-pink);
    color: #000;
  }

  a {
    color: var(--neon-blue);
    text-decoration: none;
    transition: color 0.3s ease;
  }

  a:hover {
    color: var(--accent);
    text-shadow: 0 0 6px var(--accent);
  }
</style>
